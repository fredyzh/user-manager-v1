{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Server",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/app/cmd",
            "args": [
                "-conf=${workspaceFolder}/app/configs/config.yaml",
            ]
          }
    ]
}